{
  "name": "rtc-client",
  "version": "1.0.0",
  "description": "Remote terminal client that executes commands via Socket.IO - Similar to Electron app but without GUI dependencies",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "remote",
    "terminal",
    "socket.io",
    "command",
    "execution",
    "client"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "socket.io-client": "^4.8.3"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
